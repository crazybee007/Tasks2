~~~html
<!DOCTYPE html> 声明为 HTML5 文档
<html> 元素是 HTML 页面的根元素
<head> 元素包含了文档的元（meta）数据，如 <meta charset="utf-8"> 定义网页编码格式为 utf-8（由于在大部分浏览器中直接输出中文会出现乱码，所以要在头部将字符声明为UTF-8）
<title> 元素描述了文档的标题
<body> 元素包含了可见的页面内容
<h1> 元素定义一个大标题
<p> 元素定义一个段落
~~~

##### HTML基础

网页：HTML文件,`文字`、`图像`、`超链接、`音频、视频以及Flash等

超文本标记语言：1.超越了文本限制；2.超链接文本

web标准：结构（写到HTML）、表现（写到CSS）、行为（写到JavaScript）

###### HTML语法规范

1.由尖括号包围的关键词

2.通常成对出现——双标签

特殊的单个出现——单标签

标签关系：包含、并列

###### HTML基本结构标签

头部、标题、主题

###### 开发工具

vscode的使用

###### 文档类型声明标签

<!DOCTYPE html>文档类型声明，用于告诉浏览器使用哪种HTML版本来显示网页

###### lang语言种类

定义当前文档显示语言，例如，en英语，zh-CN中文

###### 字符集

便于识别和储存各种文字

<meta charset="UTF-8"/>
没有会出现乱码的情况

###### 标签语义

标题标签

~~~html

<h1>这是标题 1</h1>
<h2>这是标题 2</h2>
<h3>这是标题 3</h3>
<h4>这是标题 4</h4>
<h5>这是标题 5</h5>
<h6>这是标题 6</h6>
~~~

段落标签

~~~html
<p>这是一个段落。</p>
<p>这是一个段落。</p>
<p>这是一个段落。</p>
~~~

换行标签<br/>

###### 文本格式化标签

加粗<strong>(更推荐)或<b>

倾斜<em>（更推荐）或<i>

删除线<del>（更推荐）或<s>

下划线<ins>（更推荐）或<u>

网页布局

###### 盒子标签

大盒子<div>

小盒子<span>

###### 图像标签

<img str="图像URL"/>

~~~html
<img src="zongxp.jpg" width="640" height="640" />
~~~

str 是<img>的必须属性，用于指定图像文件的路径和文件名

alt替换文本,图像显示不出来的时候用文字替换

title提示文本,显示在图片上

width设置宽度height设置高度border图像的边框粗细

###### 路径

目录文件夹：普通文件夹

根目录：目录文件夹的第一层

###### 相对路径

| 相对路径分类 | 符号 | 说明                                                     |
| ------------ | ---- | -------------------------------------------------------- |
| 同一级路径   |      | 图形文件位于 HTML 文件同一级 如 <img src="1.png">        |
| 上一级路径   | /    | 图形文件位于 HTML 文件下一级 如 <img src="images/1.png"> |
| 下一级路径   | ../  | 图形文件位于 HTML 文件上一级 如 <img src="../1.png">     |

###### 绝对路径

绝对路径：是指目录下的绝对位置，直接达到目标位置，通常是从盘符开始的路径

##### 超链接标签

<a href="跳转目标"target="目标窗口的弹出方式">文本或图像</a>

~~~html
<a href="https://blog.csdn.net/zong596568821xp">这是一个链接使用了 href 属性</a>
~~~

href 指定链接目标的url地址

target指定链接页面的打开方式(目标网页的打开形式)

| 取值   | 效果                                   |
| ------ | -------------------------------------- |
| _self  | 默认值，在当前窗口中跳转（覆盖原网页） |
| _blank | 在新窗口中跳转（保留原网页）           |

###### 1.外部链接

~~~html
<a href="http://www.qq.com">腾讯</a>
~~~

###### 2.内部链接

网站内部页面之间的相互链接，直接链接内部页面名称即可

~~~html
<a href="图解">图片</a>
~~~

###### 3.空链接

~~~html
<a href="#">链接</a>
~~~

###### 4.下载链接

如果href里面地址是一个文件或者压缩包，会下载这个文件

###### 5.网页元素链接

网页中的各种网页元素

~~~html
<a href="http://www.baidu.com"><img src="图解"/></a
~~~

###### 6.锚点链接

点击链接，可以快速定位到页面中的某个位置

在链接文本的href属性中，设置属性值为**#名字**的形式

~~~html
<a href="#two">第二集</a>
~~~

找到目标位置标签，里面添加一个id属性=刚才的名字

~~~html
<h3 id="two">
    第2集介绍
</h3>
~~~

##### 注释和特殊字符

###### 注释

HTML中的注释以<!--"开头，以"-->结束。

~~~html
<!--注释语句-->      快捷键：ctrl + /
~~~

###### 特殊字符

![联想截图_20231204235508](C:\Users\86152\Pictures\联想截图\联想截图_20231204235508.png)

~~~html
讲&nbsp(有多少个就显示多少个空格)究
~~~

结果为：讲 究

##### 表格标签

主要用于显示、展示数据

基本语法如下：

| 标签名 | 说明                       |
| ------ | -------------------------- |
| table  | 表格整体，可用于包裹多个tr |
| tr     | 表格每行，可用于包裹td     |
| td     | 表格单元格，可用于包裹内容 |

~~~html
<table>
    <tr>
        <td>单元格内的文字</td>
        ...
    </tr>
    ...
</table>
~~~

1.<table></table>是用于定义表格的标签。

2.<tr></tr>标签用于定义表格中的行，必须嵌套在<table></table>标签中。

3.<td></td>用于定义表格中的单元格，必须嵌套在<tr></tr>标签中。

4.td=table data,即数据单元格的内容。

表头单元格标签

**表头单元格**位于表格的**第一行或者第一列**，表头里面的**文本内容加粗居中显示**，标签表示[HTML表格](https://so.csdn.net/so/search?q=HTML表格&spm=1001.2101.3001.7020)的表头部分

~~~html
<table>
    <tr>
        <th>姓名</th>
        ...
    </tr>
    ...
</table>
~~~

###### 表格属性

<img src="C:\Users\86152\Pictures\联想截图\联想截图_20231205000741.png" alt="联想截图_20231205000741" style="zoom:50%;" />

表头单元格标签和表格标题

| 标签名  | 名称       | 说明                                       |
| ------- | ---------- | ------------------------------------------ |
| caption | 表格大标题 | 默认在表格整体顶部居中位置显示             |
| th      | 表头单元格 | 表示一列小标题，默认内部文字加粗并居中显示 |

###### 表格结构标签

| 标签名 | 名称     |
| ------ | -------- |
| thead  | 表格头部 |
| tbody  | 表格主体 |
| tfoot  | 表格底部 |

###### 合并单元格

合并方式

- 跨行合并：rowspan=“合并单元格的个数”（垂直合并）
- 跨列合并：colspan=“合并单元格的个数”（水平合并）

目标单元格（写合并代码的地方）

- 跨行：最上侧单元格为目标单元格，写合并代码
- 跨列：最左侧单元格为目标单元格，写合并代码

注意：只有同一个结构标签中的单元格才能合并，不能跨结构标签合并

合并步骤

1.先确定是跨行还是跨列合并。

2.找到目标单元格，写上合并方式=合并单元格的数量。通过左上原则，上下合并，保留最右的，删除其他；左右合并，保留最左的，删除其他。

 ~~~html
<td rowspan="2"></td>
 ~~~

3.删除多余的单元格。

##### 列表标签

表格主要用于布局，分为无序列表、有序列表、自定义列表

###### 无序列表(重点)

~~~html
<ul>标签表示HTML页面中项目的无序列表，一般会以项目符号呈现列表项，而列表项使用<li></li>标签定义。
~~~

基本语法如下：

~~~html
<ul>
    <li>列表项1</li>
    <li>列表项2</li>
    <li>列表项3</li>
    ...
</ul>
~~~

1.无序列表的各个列表项之间没有顺序级别之分，是并列的。

2.<ul></ul>中**只能**嵌套<li></li>,直接在<ul></ul>标签中输入其他标签或者文字的做法是不被允许的。

3.<li></li>之间相当于一个容器，可以容纳所有元素。

4.无序列表会带有自己的样式属性，但在实际使用时，我们会使用CSS来设置。

###### 有序列表

有序列表即为有排列顺序的列表，其各个列表项会按照一定的顺序排列定义。

~~~html
<ol>
	<li>列表项1</li>
    <li>列表项2</li>
    <li>列表项3</li>
    ···
<ol>
~~~

###### 自定义列表

常用于对属于的解释或描述

| 标签名 | 说明                                                         |
| ------ | ------------------------------------------------------------ |
| dl     | 表示自定义列表的整体，用于包裹dt/dd标签（只允许）            |
| dt     | 表示自定义列表的主题（内容任意）                             |
| dd     | dd前默认缩进效果，表示自定义列表的针对主题的每一项内容（内容任意） |

~~~html
<dl>
    <dt>名词1</dt>
    <dd>名词1解释1</dd>
    <dd>名词1解释2</dd>
</dl>html
~~~

<dt>和<dd>个数没有限制，经常是一个<dt>对应多个<dd>

<img src="C:\Users\86152\Pictures\联想截图\联想截图_20231205173748.png" alt="联想截图_20231205173748" style="zoom:80%;" />

##### 表单标签

表单域是一个包含表单元素的区域
在HTML标签中，``标签用于定义表单域，以实现用户信息的收集和传递
``会把它范围内的表单元素信息提交给服务器

~~~html
<form action="url地址”method="提交方式”name="表单域名称">
各种表单元素控件
</form>
~~~

###### input系列标签

通过type属性值的不同，展示不同效果

~~~html
<input type-"属性值" /> 
~~~

type属性的属性值及其描述如下:

~~~html
属性值			描述
button		定义可点击按钮(多数情况下，用于通过JavaScript启动脚本)
checkbox	定义复选框
file		定义输入字段和"浏览"按钮，供文件上传
hidden		定义隐蒙的输入字段
image		定义图像形式的提交按钮
password	定义密码字段。该字段中的字符被掩码
radio		定义单选按钮。
reset		定义重置按钮。重置按钮会清除表单中的所有数据
submit		定义提交按钮。提交按钮会把表单数据发送到服务器
text		定义单行的输入字段，用户可在其中输入文本。默认宽度为20个字符
placeholder	占位符，提示用户输入内容的文本
name		分组，有相同name属性的单选框为一组，一组中同时只能有一个被选中
value		由用户自定义，规定input元素的值
checked		默认选中
multiple	多文件选择
maxlength	正整数，规定输入字段中的字符的最大长度
按钮
submit		提交按钮，点击后提交数据给后台服务器
reset		重置按钮，点击后回复表单默认值
button		普通按钮，默认无功能，之后配合js添加功能
~~~

###### button按钮标签

~~~html
<button>这是按钮</button>
<button type="submit">提交按钮</button>
<button type="reset">重置按钮</button>
~~~

###### select下拉菜单标签

select标签：下拉菜单的整体

option标签：下拉菜单的每一项

selected：下拉菜单默认选中

###### 文本域

textarea文本域标签

~~~html
cols	规定了文本域内可见宽度、
rows	规定了文本域内可见行数
~~~

###### label标签

使用方法（1）：

1.使用label标签把内容包裹起来

2.在表格标签上添加id属性

3.在label标签的for属性中设置对应的id属性

~~~html
<input type="radio" name="sex" id="nan"><label for="nan">男</label>
~~~

使用方法（2）：

1.直接使用label标签把内容和表单标签一起包裹起来

2.需要把label标签的for属性删掉即可

~~~html
<label><input type="radio" name="sex" checked>女</label>
~~~

##### 语义化标签

###### 没有语义的标签

~~~html
div标签	一行只显示一个（独占一行）
span	一行可以显示多个
~~~

###### 有语义的标签

用于手机端，用于HTML5新版本中

~~~html
header	网页头部
nav		网页导航
footer	网页底部
aside	网页侧边栏
section	网页区块
article	网页文章
~~~

##### 字符实体

结构：&英文；

常用：

~~~html
空格	&nbsp;
~~~









